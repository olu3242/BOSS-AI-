{
  "name": "@boss/api",
  "version": "0.1.0",
  "private": true,
  "main": "./src/index.ts",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "dev": "tsx watch src/index.ts",
    "lint": "eslint src --max-warnings=0",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "vitest run",
    "clean": "rm -rf dist"
  },
  "dependencies": {
    "@boss/types": "workspace:*",
    "@boss/shared": "workspace:*",
    "@boss/mcp": "workspace:*",
    "@boss/loop": "workspace:*",
    "@boss/events": "workspace:*",
    "@boss/db": "workspace:*",
    "@boss/registries": "workspace:*",
    "@boss/industry-pack-general-smb": "workspace:*"
  },
  "devDependencies": {
    "tsx": "^4.16.0",
    "typescript": "^5.5.0",
    "vitest": "^2.0.0"
  }
}
