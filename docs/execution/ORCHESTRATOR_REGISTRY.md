# Orchestrator Registry

Certification date: 2026-06-27

`orchestratorRegistry` is implemented as a readonly, typed registry.

Registered orchestrators: **0**

API services coordinate business-intelligence calls, while `packages/loop`
contains only execution interfaces. Neither is represented as an agent
orchestrator because no orchestrator definition exists. No runtime, service,
or execution path was modified.

This registry must remain empty until an orchestrator is explicitly designed,
owned, versioned, and certified.
