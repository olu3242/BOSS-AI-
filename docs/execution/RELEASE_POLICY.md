# Release Policy

A certified capability may release only when its applicable packs and bundles
are certified, compatibility is declared, installation and upgrade pass, and
rollback restores the previous supported state. Marketplace publication must
reference immutable versions and trusted signatures.

Breaking changes require architecture approval, a migration/adapter strategy,
an explicit deprecation window, and renewed certification.
